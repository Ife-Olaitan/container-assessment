apiVersion: v1
kind: Service
metadata:
  namespace: muchtodo
  name: backend-service
  labels:
    app: backend
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - port: 8080
      targetPort: 8080